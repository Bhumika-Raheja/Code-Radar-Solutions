#include <stdio.h> 
int main(){
    int a,max=0;
    int arr[]={};
    for (int i=0;i<a;i++){
        scanf("%d",&a);
        arr[i]=a;
        if (a>count){
            count=a;
        }
    }
    return 0;
}