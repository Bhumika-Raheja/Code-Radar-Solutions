#include <stdio.h>
int main() {
    char alpha;
    scanf("%c",alpha);
    if (alpha = 'A-Z'){
        printf("Uppercase");
    }
    else if (aplha ='a-z'){
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
}